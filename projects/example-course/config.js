/*
 * 範例專案設定（projects/example-course/config.js）
 * 這是可運作的示範樣本，使用「佔位」GAS 網址（不會真的連上任何後端）。
 * 真實專案請用 scripts/new-project.sh 產生，並填入自己的 /exec 網址。
 */
window.PLAN_BOOK_CONFIG = {
  PROJECT_NAME: 'iCAP 範例課程（示範專案）',
  GAS_API_URL: 'https://script.google.com/macros/s/REPLACE_WITH_YOUR_DEPLOYMENT_ID/exec',
};
